# ============================================================
# 环境变量配置模板
# 复制为 .env 后填入真实值：cp .env.example .env
# ============================================================

# ------------------------------------------------------------
# 数据库
# ------------------------------------------------------------
# Docker Compose 内部用 postgres 服务名；本地开发用 localhost
DATABASE_URL=postgresql://mqs:mqs_dev_password@postgres:5432/mold_quote?schema=public

# ------------------------------------------------------------
# 服务端口
# ------------------------------------------------------------
PORT=3000
LOG_LEVEL=info

# ------------------------------------------------------------
# 认证
# ------------------------------------------------------------
# 生产环境 MUST 修改！生成方式：openssl rand -hex 32
JWT_SECRET=dev-secret-change-in-production-please

# ------------------------------------------------------------
# 邮件服务（QQ 邮箱 SMTP）
# ------------------------------------------------------------
# QQ 邮箱 SMTP 授权码获取：
#   登录 QQ 邮箱 → 设置 → 账号 → POP3/IMAP/SMTP/Exchange/CardDAV/CalDAV服务
#   → 开启「IMAP/SMTP服务」→ 生成授权码
# 注意：pass 填的是「授权码」，不是 QQ 登录密码
SMTP_HOST=smtp.qq.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=729503962@qq.com
SMTP_PASS=lbukpgyzzmtqbcfl
SMTP_FROM=模具注塑报价系统 <729503962@qq.com>

# ------------------------------------------------------------
# 公网访问地址（用于生成报价单分享链接）
# ------------------------------------------------------------
# 本地开发
# PUBLIC_WEB_URL=http://localhost:5173
# 生产环境（域名）
PUBLIC_WEB_URL=https://wycl.chat

# ------------------------------------------------------------
# 前端（Vite 构建时注入）
# ------------------------------------------------------------
VITE_API_URL=http://localhost:3000

# ------------------------------------------------------------
# PostgreSQL（Docker Compose 用）
# ------------------------------------------------------------
POSTGRES_USER=mqs
POSTGRES_PASSWORD=mqs_dev_password
POSTGRES_DB=mold_quote
